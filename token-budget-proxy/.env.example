# Only needed to run against the real API. The demo defaults to --upstream mock,
# which simulates the parts of `usage` this tool reasons about and needs no key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# TBP_UPSTREAM=https://api.anthropic.com
# TBP_POLICY=policy.yaml
# TBP_DB=tbp.db
